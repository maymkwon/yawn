// 컬러정의 입니다.

const defaultColor = {
  red: '#000',
  blue: '#000',
  green: '#000'
}

const color = {
  primary: defaultColor.red,
  secondary: defaultColor.red,
  warning: defaultColor.red,
  danger: defaultColor.red,
}

export default color